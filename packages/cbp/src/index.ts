// @agentlair/cbp — reserved namespace stub.
// CBP (Capital Bond Pool). v1 with the actual primitive ships when the bond contract is audited and deployed on Base mainnet, and the verifier API at /v1/cbp/oracle/<address> is live.
// Spec: https://agentlair.dev/specs/cbp

export const VERSION = "0.0.1";
export const STATUS = "reserved" as const;
export const PRIMITIVE = "CBP" as const;
export const EXPANSION = "Capital Bond Pool" as const;
export const SPEC_URL = "https://agentlair.dev/specs/cbp" as const;
