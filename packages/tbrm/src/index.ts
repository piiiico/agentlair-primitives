// @agentlair/tbrm — reserved namespace stub.
// TBRM (Tracked Brier Reputation Metric). v1 with the actual primitive ships when the BCC v1 schema is published and the verifier API at /v1/bptr/oracle/<did> exposes external Brier reads.
// Spec: https://agentlair.dev/specs/tbrm

export const VERSION = "0.0.1";
export const STATUS = "reserved" as const;
export const PRIMITIVE = "TBRM" as const;
export const EXPANSION = "Tracked Brier Reputation Metric" as const;
export const SPEC_URL = "https://agentlair.dev/specs/tbrm" as const;
